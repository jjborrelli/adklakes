---
title: "Adirondack Lakes and Ponds"
output: bookdown::html_document2
date: "2023-04-10"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE, comment = NA)
```

```{r}
library(readxl)

library(dplyr)
library(tidyr)
library(lubridate)

library(sf)

library(ggplot2)
library(ggridges)
library(patchwork)

library(rLakeAnalyzer)
```

Re-look at focal research questions and see what I can address

Here I provide an overview of data available to analyze for the SCALE project. 

Future SCALE sampling efforts will be tiered with different intensity levels depending its level of priority. All lakes will be sampled using remote sensing, 300 will be sampled once for basic characteristics, 50 will be sampled at intermediate levels (more variables, several sampling trips), and 25 will be sampled intensely (i.e., monitoring buoys, multiple sampling trips, etc) Our goal is to categorize which lakes should be sampled with which degree of intensity. We want to ensure that each sampling intensity level covers a range of waterbody types and characteristics found in the Adirondacks.  

# Waterbodies in the Adirondack Park

The first step is to determine the basic characterisitics of all waterbodies in the park. To do that we turn to the [National Hydrography Dataset](https://www.usgs.gov/national-hydrography/national-hydrography-dataset) (NHD). We look at all waterbodies in the NY region, and then filter out those that lie within the boundary of the Adirondack Park (Figure \@ref(fig:nyWATERBODIES)).

```{r}
states <- st_as_sf(maps::map("state", plot = FALSE, fill = TRUE))

# Adirondack outline
# https://geodata.lib.utexas.edu/catalog/cugir-007739
adkalt <- st_read("C:/Users/borre/Downloads/cugir-007739/cugir-007739/", "blueline", quiet = TRUE)

adkwater <- readRDS("nhd/adk_nhd2.rds")

adkwaterbd <- readRDS("nhd/adk_nhd_wb12_adk.rds")
adkwaterbd10 <- readRDS("nhd/adk_nhd_wb10_adk.rds")


# test <- readRDS("nhd/adk_nhd.rds")
```

```{r nyWATERBODIES, out.width = "50%", fig.cap = "Waterbodies in NY (grey) with those within the ADK park highlighted (blue)", fig.align = 'center'}
knitr::include_graphics("newyorkwater.png")
```

The NHD lists nearly 11215 waterbodies in their dataset. Of those, only 1750 have GNIS ID numbers and names. A fun fact is that the most common name among lakes and ponds in the Adirondack Park is **Mud Pond**. 

These lakes and ponds range from as small as 24 m^2^ to as large as 1121 km^2^. The NHD also includes elevation for 1115 of the lakes and ponds. The lowest elevation is an unnamed lake (0.12 km^2^) at 28.3 m, while the highest elevation in the dataset is for Woodworth Lake (0.138 km^2^) at 1680 m. 

The quantiles for lakes and pond surface area (ha) are: `r round(quantile(adkwater$areasqkm, probs = seq(0,1,.1))*100, 2)`

90% of lakes and ponds are less than 7.4 ha. Of those that are larger the quantiles (0.91-0.99 by 0.01) are: `r round(quantile(adkwater$areasqkm, probs = seq(0.91,0.99,.01))*100, 1)`



From the NHD we can also use data on watershed boundaries. The NHD includes multiple watershed boundaries based on several hydrological units. We are looking at HU10 (Figure \@ref(fig:adkhu10)), which includes a major waterbody.

```{r adkhu10, out.width = "50%", fig.cap = "Waterbodies in ADK with watershed boundaries at the HU10 level", fig.align = 'center'}
knitr::include_graphics("adkwaterboundary_hu10.png")
```
 
## Land Cover

To determine the types of land cover around each waterbody we turn to the [National Land Cover Database](https://www.usgs.gov/centers/eros/science/national-land-cover-database) (NLCD; Figure \@ref(fig:adknlcd)). The NLCD gives land cover estimates for 30 m by 30 m grid cells. 

```{r adknlcd, fig.cap = "Land cover in the Adirondack Park", fig.align = 'center'}
knitr::include_graphics("adklandcover.png")
```

Over the whole park, the most common land cover type is deciduous forest (46%), followed by evergreen forest (21%), mixed forest (12%), and woody wetlands (9%; Figure \@ref(fig:adklcbar)). About 5.6% of the park is open water. 

```{r adklcbar, out.width = "50%", fig.cap = "Land cover percentages in the Adirondack Park", fig.align = 'center'}
knitr::include_graphics("adklandcoverBARPLOT.png")
```


# Remote Sensing

*NLDAS data here*
```{r}
nldas <- read.csv("data/ADK_Data/Data/nldas_adk_monthly.csv")
adklt <- read.csv("C:/Users/borre/Downloads/adk_lt_trends.csv")
```

```{r, eval = FALSE}
test <- nldas %>% select(-X) %>%
  gather(key = var, value = value, TMP_1, SPFH_1:PRES_1, 
         PEVAP_1, wind_spd, wind_dir) %>% 
  group_by(lon, lat, var) %>% nest() %>% 
  filter(var == "TMP_1") %>% 
  mutate(fit = purrr::map(data, ~lmer(value ~ year + (year | month), data = .x)))

summary(test$fit[[1]])
nldas2 <- nldas
nldas2$time <- as.numeric(ymd(paste(nldas$year, nldas$month, "15", sep = "-")))
test2 <- gam(TMP_1 ~ s(year) + s(month, bs = "cc") + s(lon,lat), data = nldas)
test3 <- gam(TMP_1 ~ s(time) + s(lat,lon), data = nldas2)
summary(test2)
summary(test3)

gratia::draw(test2)
gratia::draw(test3)

df <- expand.grid(year = 1985:2020, month = 1:12, lat = c(43.79896), lon = c(-73.91391))

broom::augment(test2, newdata = df) %>% 
  ggplot(aes(x = ymd(paste(year, month, "15", sep = "-")), y = .fitted)) + 
  geom_line()

broom::augment(test2, newdata = df) %>% 
  group_by(year) %>% filter(month %in% 6:8) %>% 
  summarize(maxtemp = max(.fitted), meantemp = mean(.fitted)) %>% 
  ggplot(aes(x = year, y = meantemp)) + geom_point()

p <- broom::augment(test2) %>% 
  mutate(date = ymd(paste(year, month, "15", sep = "-"))) %>% 
  ggplot(aes(x = lon, y = lat, fill = .fitted)) + 
  geom_tile() + 
  transition_states(date) +
  labs(title = "{closest_state}")
animate(p, nframes = 1000)  


p2 <- nldas %>% 
  mutate(date = ymd(paste(year, month, "15", sep = "-"))) %>% 
  ggplot(aes(x = lon, y = lat, fill = TMP_1)) + 
  geom_tile() + 
  transition_states(date) +
  labs(title = "{closest_state}")
animate(p2, nframes = 1000)  

# era5 <- arrow::read_parquet("data/era5_adk_1992-2012.parquet")
# 
# era5 %>% filter(datetime == ymd_hms("1992-01-01 00:00:00")) %>% 
#   ggplot(aes(x = lat, y = lon, fill = Air_Temperature_celsius)) +
#   geom_tile()

p3 <- era5 %>% 
  mutate(date = floor_date(datetime, "1 month")) %>%
  group_by(lat, lon, date) %>% 
  summarize(TMP_1 = mean(Air_Temperature_celsius)) %>% 
  ggplot(aes(x = lon, y = lat, fill = TMP_1)) + 
  geom_tile() + 
  transition_states(date) +
  labs(title = "{closest_state}")
animate(p3, nframes = 1000)  


era5 %>% 
  mutate(date = floor_date(datetime, "1 month")) %>%
  group_by(lat, lon, date) %>% 
  summarize(TMP_1 = mean(Air_Temperature_celsius)) %>% 
  filter(month(date) == 7) %>% 
  ggplot(aes(x = date, y = TMP_1)) + geom_point() + geom_smooth(method = "lm") + 
  facet_grid(lat ~ lon)

```


# Historical Monitoring

## Brief review of historical monitoring

Adirondack water bodies have been the subject of numerous long- and short-term monitoring programs. 

## Ice cover

For 8 lakes in the Adirondacks we have a record of ice-in and ice-out from 1874 through 2022. Ice-out records show a generally declining trend, with ice-out occurring earlier each year (Figure \@ref(fig:iceout)). There does not, however, seem to be any apparent trend in the duration of ice cover in these lakes, though the variance in duration may be increasing (Figure \@ref(fig:icecover)).   

```{r}
icecov <- read.csv("data/lake_ice_1874-2022.csv")
```

```{r iceout, fig.cap = "The ice out date for each year for each of 8 monitored lakes", fig.align='center'}
ggplot(icecov, aes(x = year(ymd(date_ice_out)), y = julian_day_out, color = duration_day)) +
  geom_point() + facet_wrap(~lake, ncol = 2) + scale_color_viridis_c() + 
  theme_bw() + 
  labs(x = "Year", y = "Day of year", 
       color = "Ice cover\nduration")
```

```{r icecover, fig.cap = "The period of ice cover for each of 8 monitored lakes. Zero indicates the start of the year (Jan 1), with negative values being days prior to the start of the year.", fig.align='center'}
icecov %>% 
  mutate(doy_start = case_when(julian_day_in > 240 ~ julian_day_in - 365, TRUE ~ julian_day_in)) %>% 
  ggplot(aes(x = year(ymd(date_ice_out)), ymin = doy_start, ymax = julian_day_out)) + geom_linerange() +
  facet_wrap(~lake, ncol = 2) + 
  theme_bw() + 
  labs(x = "Year", y = "Day of year")
```


## Adirondack Lake Survey

The Adirondack Lake Survey (ALS). 

### 1984-1987

The 1984-1987 ALS data can be found on [Figshare here](https://figshare.com/articles/dataset/Adirondack_Lakes_Survey_1984-1987_Complete_Data_Set/22312732). It includes tables on location, status, morphometrics, substrate, watershed characteristics, inlets and outlets, macroinvertebrates, aquatic vegetation, physical parameters, gear type for fishing, individual and community fish data, water chemistry, liming, and reclamation.  

```{r}
als_location <- read_excel("data/ALS84/REC1_DATA_TABLE.xls")
als_status <- read_excel("data/ALS84/REC2_DATA_TABLE.xls")
als_morpho <- read_excel("data/ALS84/REC3_DATA_TABLE.xls")
als_substr <- read_excel("data/ALS84/REC4_DATA_TABLE.xls")
als_wshed <- read_excel("data/ALS84/REC5_DATA_TABLE.xls")
als_inout <- read_excel("data/ALS84/REC6_DATA_TABLE.xls")
als_macinv <- read_excel("data/ALS84/REC7_DATA_TABLE.xls")
als_aqveg <- read_excel("data/ALS84/REC8_DATA_TABLE.xls")
als_physic <- read_excel("data/ALS84/REC9_DATA_TABLE.xls")
als_gear <- read_excel("data/ALS84/REC10_DATA_TABLE.xls")
als_indfish <- read_excel("data/ALS84/REC11_DATA_TABLE.xls")
als_fishcomp <- read_excel("data/ALS84/REC12_DATA_TABLE.xls")
als_wchem <- read_excel("data/ALS84/REC13_DATA_TABLE.xls")
als_stock <- read_excel("data/ALS84/REC14_DATA_TABLE.xls")
als_liming <- read_excel("data/ALS84/REC15_DATA_TABLE.xls")
als_reclam <- read_excel("data/ALS84/REC16_DATA_TABLE.xls")
```

```{r alsMORPHO, fig.align = "center", fig.height = 6, fig.width = 5, fig.cap = "ALS lake elevation, volume, watershed area, surface area, max depth, and mean depth"}
elev1 <- ggplot(als_morpho, aes(x = ELEV)) + geom_histogram()
warea1 <- ggplot(als_morpho[als_morpho$WAREA > 0,], aes(x = WAREA)) + geom_histogram() + scale_x_log10()
sarea1 <- ggplot(als_morpho, aes(x = SAREA)) + geom_histogram() + scale_x_log10()
vol1 <- ggplot(als_morpho, aes(x = VOLUME)) + geom_histogram() + scale_x_log10()
maxdep1 <- ggplot(als_morpho, aes(x = MAXDEPTH)) + geom_histogram() + scale_x_log10()
meandep1 <- ggplot(als_morpho, aes(x = MEANDEPTH)) + geom_histogram() + scale_x_log10()

(elev1 | vol1) / (warea1 | sarea1) / (maxdep1 | meandep1)
```

```{r}
ggplot(filter(als_morpho, MEANDEPTH > 0), aes(x = MEANDEPTH/MAXDEPTH)) + geom_histogram() #+ scale_x_log10()
```


```{r tempPROF, fig.align = "center", fig.height = 6, fig.width = 6, fig.cap = "ALS temperature profiles by month"}
ggplot(als_physic, aes(x = DEPTH, y = TEMPF)) + geom_point(alpha = 0.2) + 
  geom_line(aes(group = PONDNO), alpha = 0.3) + 
  geom_smooth() + 
  scale_x_reverse() + 
  coord_flip() + 
  facet_wrap(~month(dmy(ADATE)), nrow = 2)
```

```{r tempPROF, fig.align = "center", fig.height = 6, fig.width = 6, fig.cap = "ALS temperature profiles by month"}
ggplot(als_physic, aes(x = DEPTH, y = DO)) + geom_point(alpha = 0.2) + 
  scale_x_reverse() + 
  coord_flip() + 
  facet_wrap(~month(dmy(ADATE)), nrow = 2)

do1 <- als_physic %>% filter(!is.na(DO)) %>% 
  group_by(PONDNO, ADATE) %>% 
  mutate(mdep = max(DEPTH)) %>% 
  filter(DO <= 5) %>%
  summarize(lowDO = n(), ldoDEP = min(DEPTH), md = max(mdep)) %>% 
  ungroup() %>% group_by(PONDNO) %>% 
  summarize(lowDOdays = sum(lowDO > 0), lowDO = sum(lowDO), dep = min(ldoDEP), mdep = max(md)) %>% 
  left_join(als_morpho, by = "PONDNO") %>% select(PONDNO:mdep, MAXDEPTH)
ggplot(do1, aes(x = dep/MAXDEPTH)) + geom_histogram()

do1 <- als_physic %>% filter(!is.na(DO)) %>% 
  group_by(PONDNO, ADATE) %>% 
  filter(DO <= 5) %>%
  summarize(lowDO = n(), ldoDEP = min(DEPTH))
ggplot(do1, aes(x = ldoDEP)) + geom_histogram() + facet_wrap(~month(dmy(ADATE)))

```

```{r}
als_physic %>% 
  group_by(PONDNO, mo = month(dmy(ADATE)), STATION) %>% 
  summarize(th = thermo.depth(TEMPF, DEPTH), dep = max(DEPTH),
            secchi = mean(SECCHI, na.rm = TRUE), diff = max(diff(TEMPF))) %>% 
  left_join(als_morpho, by = "PONDNO") %>% 
  filter(mo %in% 7:8, diff < 0) %>% 
  mutate(mo = case_when(mo == 7 ~ "July", TRUE ~ "Aug"), 
         mo = factor(mo, levels = c("July", "Aug"))) %>% 
  ggplot(aes(x = dep, y = th, color = log10(MEANDEPTH))) + geom_point() + 
  geom_abline(intercept = 0, slope = 1, lty = 2) +
  facet_wrap(~mo)
```


```{r, fig.align = "center", fig.height = 4, fig.width = 6, fig.cap = "August thermocline depth by surface area"}
als_physic %>% filter(month(dmy(ADATE)) == 8) %>% 
  filter(!is.na(TEMPF), year(dmy(ADATE)) %in% 1984:1987) %>% 
  group_by(ADATE, PONDNO, STATION, DEPTH) %>% 
  summarize(TEMPF = mean(TEMPF, na.rm = TRUE)) %>%
  ungroup() %>% 
  group_by(ADATE, PONDNO, STATION) %>% 
  summarize(
    th = thermo.depth(TEMPF, DEPTH)
  ) %>% 
  left_join(als_morpho, by = "PONDNO") %>% 
  filter(MAXDEPTH > 0) %>% 
  ggplot(aes(x = SAREA, th, color = MAXDEPTH)) + geom_point() + 
  #scale_y_log10() + 
  scale_x_log10() + 
  labs(x = "Log10 Surface Area", y = "Aug Thermocline Depth")

```


```{r doPROF, fig.align = "center", fig.height = 6, fig.width = 4, fig.cap = "ALS lake dissolved oxygen profiles by month"}
ggplot(als_physic, aes(x = DEPTH, y = DO)) + geom_point(alpha = 0.2) + 
  # geom_line(aes(group = PONDNO), alpha = 0.3) + 
  scale_x_reverse() + 
  coord_flip() + 
  facet_wrap(~month(dmy(ADATE)), nrow = 2)
```

```{r wchem, fig.align = "center", fig.height = 6, fig.width = 8, fig.cap = "ALS lake chemistry at 1.5 m"}
als_wchem %>% 
  filter(DEPTH == 1.5) %>% 
  mutate(ADATE = dmy(ADATE)) %>% 
  dplyr::select(PONDNO:ADATE, SO4, NO3, CL, ANC, DIC, DOC, TOTALP1, CA, NH4, FE, TRUCOLOR, SCONDUCT) %>% 
  tidyr::gather(key = var, value = value, SO4:SCONDUCT) %>% 
  ggplot(aes(x = value)) + geom_histogram() + facet_wrap(~var, scales = "free") 

als_wchem %>% 
  filter(DEPTH == 1.5) %>% 
  mutate(ADATE = dmy(ADATE)) %>% 
  dplyr::select(PONDNO:ADATE, LABPH, FIELDPH, MG, `NA`, K, AL, MN, PB, ZN) %>% 
  tidyr::gather(key = var, value = value, LABPH:ZN) %>% 
  ggplot(aes(x = value)) + geom_histogram() + facet_wrap(~var, scales = "free") 
```

```{r}
cmat <- als_wchem %>% 
  filter(DEPTH == 1.5) %>% 
  mutate(ADATE = dmy(ADATE)) %>% 
  dplyr::select(PONDNO:ADATE, SO4, NO3, CL, ANC, DIC, DOC, TOTALP1, CA, NH4, FE, TRUCOLOR, SCONDUCT) %>% 
  group_by(PONDNO) %>% 
  summarize_at(vars(SO4:SCONDUCT), mean, na.rm = TRUE) %>% ungroup() %>% 
  dplyr::select(-PONDNO) %>% as.matrix()

princomp(scale(cmat[complete.cases(cmat), ])) %>% 
  biplot(cex = c(0.3, 1))
princomp(scale(cmat[complete.cases(cmat), ])) %>% 
  summary()
```



```{r, fig.align = "center", fig.height = 4, fig.width = 4, fig.cap = "Chloride in the upper 1.5 m of ADK lakes and ponds"}
als_wchem %>% filter(DEPTH %in% c(0, 1, 1.5)) %>% 
  ggplot(aes(x = DEPTH, y = CL, group = DEPTH)) + geom_boxplot() + scale_y_log10()
```


```{r}
subpc <- als_substr %>%  select(PONDNO:ADATE, BEDROCK:OTHER) %>% 
  gather(key = substr, value = value, BEDROCK:OTHER) %>% 
  filter(!is.na(value)) %>% 
  group_by(PONDNO) %>% mutate(value = value/sum(value)) %>% 
  spread(key = substr, value = value, fill = 0) %>% 
  ungroup() %>% 
  select(BEDROCK:OTHER) %>% princomp()
```


```{r, fig.align = "center", fig.height = 5, fig.width = 5, fig.cap = "Biplot of substrate type PCA on 1467 lakes/ponds"}
biplot(subpc, cex = c(0.5, .75), col = c("grey", "blue"))
```



```{r}
fshCODE <- read.csv("data/ALS84/ADKfishcodes.csv")

ifishSUM <- als_indfish %>% 
  group_by(PONDNO, STATION, ADATE, SPECIES) %>% 
  summarize(n = n(), len = mean(LENGTH), wgt = mean(WEIGHT)) %>% 
  left_join(fshCODE, by = c("SPECIES" = "SPP_CODE")) %>% arrange(desc(n))
```

```{r fishpc, fig.align = "center", fig.height = 5, fig.width = 5, fig.cap = "Biplot of PCA on relative abundance of fish species in 1023 lakes"}
ifishSUM %>% 
  ungroup() %>% group_by(PONDNO, STATION, SPP_LAB) %>% 
  summarize(n = mean(n)) %>% 
  ungroup() %>% group_by(PONDNO, SPP_LAB) %>%
  summarize(n = mean(n)) %>%
  ungroup() %>% group_by(PONDNO) %>%
  mutate(n = n/sum(n)) %>%
  spread(key = SPP_LAB, value = n, fill = 0) %>% 
  ungroup() %>% select(-PONDNO) %>% 
  princomp() %>% biplot(cex = 0.75, col = c("lightblue", "black"))
```


### 1992 ALTM

[ALTM data](https://portal.edirepository.org/nis/mapbrowse?packageid=edi.37.1)

```{r}
altm <- read.csv("data/NY_ADIRONDACK_1992.csv")
```

```{r, fig.align = "center", fig.height = 10, fig.width = 10, fig.cap = "DOC over time in ALTM lakes"}
altm %>% 
  mutate(date = ymd(Sample_Date)) %>% 
  filter(!is.na(DOC), Depth == 0) %>% 
  ggplot(aes(x = date, y = DOC)) + geom_point() + 
  facet_wrap(~ALSC_Site_ID, scales = "free_y")
```

```{r, fig.align = "center", fig.height = 10, fig.width = 10, fig.cap = "Chloride over time in ALTM lakes"}
altm %>% 
  mutate(date = ymd(Sample_Date)) %>% 
  group_by(ALSC_Site_ID) %>% mutate(n = n()) %>% 
  filter(n > 10) %>% 
  filter(!is.na(Cl), Depth == 0) %>% 
  ggplot(aes(x = date, y = Cl)) + geom_point() + 
  facet_wrap(~ALSC_Site_ID, scales = "free_y")
```

```{r, fig.align = "center", fig.height = 10, fig.width = 10, fig.cap = "Chlorophyll over time in ALTM lakes"}
altm %>% 
  mutate(date = ymd(Sample_Date)) %>% 
  filter(!is.na(Chla), Depth == 0) %>% 
  group_by(ALSC_Site_ID) %>% mutate(n = n()) %>%
  filter(n > 10) %>% #select(ALSC_Site_ID, n) %>% arrange(n)
  ggplot(aes(x = date, y = Chla)) + geom_point() + 
  facet_wrap(~ALSC_Site_ID, scales = "free_y")
```

### 1994-2005 Fish  

The 1994-2005 ALS fisheries data can be found on [Figshare here](https://figshare.com/articles/dataset/ALTM_Fisheries_Dataset_1994-2005/22317337).

```{r}
alsc94FISH1 <- read_excel("data/ALS94/LTM_FISH_ DATA_94-2005.xls", sheet = 2, skip = 3)
alsc94FISH2 <- read_excel("data/ALS94/LTM_FISH_ DATA_94-2005.xls", sheet = 3, skip = 3)
```


### 2008-2012 Fish 

The 2008-2012 ALS fisheries data can be found on [Figshare here](https://figshare.com/articles/dataset/ALTM_Fisheries_Dataset_2008-2012/22329316).



## AEAP

```{r}
library(adklakedata)

aeapchem <- adk_data("chem")
```

There are 28 lakes in the AEAP dataset, obtained through the R package `adklakedata`. The package includes datasets on chemistry, crustacean zooplankton, rotifer zooplankton, phytoplankton, lake nutrients, secchi depth, temperature and dissolved oxygen profiles, metadata, and lake-specific meteorology.  

```{r, fig.align = "center", fig.height = 8, fig.width = 10, fig.cap = "DOC over time in AEAP lakes"}
aeapchem %>% 
  ggplot(aes(x = ymd(date), y = DOC)) + geom_point() + 
  facet_wrap(~lake.name) + labs(x = "Date")
```


## Citizen's Statewide Lake Assessment Program 

The Citizen's Statewide Lake Assessment Program (CSLAP) is a volunteer-based monitoring program for lakes around New York State. CSLAP is managed by the NY Department of Environmental Conservation ([NYDEC](https://www.dec.ny.gov/)) and the New York State Federation of Lake Associations ([NYSFOLA](https://nysfola.org/)). Citizen volunteers collect samples that are analyzed by state (or state-approved) labs.   

Of the 259 lakes in the program there are about 52 lakes in CSLAP within the Adirondack Park, with 72 total sampling locations (Figure \@ref(fig:cslapmap)). These data ([available on EDI here](https://portal.edirepository.org/nis/mapbrowse?packageid=edi.39.1)) run from June of 1986 through July 2012, though additional data may be available elsewhere.  

```{r}
cslap <- read.csv("data/NY_CSLAP.csv")
```


```{r cslapmap, fig.align = "center", fig.cap = "New York lakes involved in the CSLAP program, with the ADK park highlighted."}
ggplot(st_transform(adkalt, st_crs(adkwater))) +  
  geom_sf(data = filter(states, ID == "new york")) +
  geom_sf(fill = "green4", alpha = 0.5) +
  geom_point(data = cslap, aes(x = CorLong, y = CorLat)) + 
  theme_minimal() 
```

These CSLAP data include information on each lake and sampling point, as an ID number assigned by NYDEC, an official lake name, and a NYS pond number. Other metadata include a NYS volume code, surface area, bottom depth, sample date, sample depth, sample type, latitude, and longitude. Samples are typically taken either 1.5 m below the surface or 1.5 m above the bottom, though other sample depths are included. The chemistry data include total phosphorus, NOx (nitrite and nitrate), NH4, total nitrogen, and chlorophyll. Samples also include secchi depth, temperature, and color.  


```{r}
pnts_sf <- st_as_sf(cslap, coords = c("CorLong", "CorLat"), crs = st_crs("WGS84"))
cslap_adk <- st_intersection(pnts_sf, st_transform(adkalt, "WGS84"))
```

The surface area of lakes in CSLAP within ADK park range from 0.1 km^2^ to 114 km^2^
```{r cslapSA, fig.cap = "Distribution of lake surface area in CSLAP lakes within ADK park", fig.align = "center", fig.height = 3, fig.width = 3}
ggplot(cslap_adk, aes(x = Areal * 0.01)) + geom_histogram(binwidth = 2.5) +
  theme_bw() + 
  labs(x = "Surface Area (km2", y = "Count")
```


A look at general trends across all ADK CSLAP lakes suggest that nutrients (TN, TP) are remaining relatively flat, temperature is rising, while chlorophyll and color are decreasing and sechhi depth is getting deeper (Figure \@ref(fig:trendCSLAP)). The trend in secchi and color seem to buck trends observed in more generalized monitoring. which typically indicate shallower secchi depth due to darkening lakes (with higher DOC as a result of reduced acidification). 

```{r trendCSLAP, fig.cap = "Secchi depth, color, total phosphorus, total nitrogen, and chlorophyll a in CSLAP lakes within the Adirondack Park (at 1.5 m for all but Secchi). Lines are output from a Generalized Additive Model.", fig.align = "center", fig.height = 8, fig.width = 6}
czsd <- cslap_adk %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(Zsd))) + geom_point() +
  geom_smooth(method = "gam") + 
  scale_y_reverse() +
  theme_bw() + 
  labs(x = "Date", y = "Secchi Depth (m)")

ctp <- cslap_adk %>% 
  filter(Zsamp == "1.5", tolower(Type) == "surface") %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(Tot_P))) + geom_point() +
  geom_smooth(method = "gam") + 
  theme_bw() + 
  labs(x = "Date", y = "Total P (mg/L)")

ctn <- cslap_adk %>% 
  filter(Zsamp == "1.5", tolower(Type) == "surface") %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(TN))) + geom_point() +
  geom_smooth(method = "gam") + 
  theme_bw() + 
  labs(x = "Date", y = "Total N (mg/L)")

cchla <- cslap_adk %>% 
  filter(Zsamp == "1.5", tolower(Type) == "surface") %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(Chl_a))) + geom_point() +
  geom_smooth(method = "gam") + 
  theme_bw() + 
  labs(x = "Date", y = "Chlorophyll a (ug/L)")

ccol <- cslap_adk %>% 
  filter(Zsamp == "1.5", tolower(Type) == "surface") %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(TColor))) + geom_point() +
  geom_smooth(method = "gam") + 
  theme_bw() + 
  labs(x = "Date", y = "Color (PCU)")

ctem <- cslap_adk %>% 
  filter(Zsamp == "1.5", tolower(Type) == "surface") %>% 
  ggplot(aes(x = mdy(Date), y = as.numeric(TH20))) + geom_point() +
  geom_smooth(method = "gam") + 
  theme_bw() + 
  labs(x = "Date", y = "Temperature (°C)")

(czsd | ccol) / (ctp | ctn) / (cchla | ctem)
```


## Lake multi-scaled Geospatial and temporal database

The LAke multi-scaled GeOSpatial and temporal database (LAGOS-NE-LIMNO v1.087.3) is a data package that includes information on lakes and reservoirs in 17 lake-rich states in the Northeastern and upper Midwestern United States. The LIMNO part of the LAGOS-NE database includes in-situ measurements of lake water quality over the past 30 years, taken from federal, state, tribal, and non-profit agencies. Overall it covers 2600-12000 lakes (dependent on variable).  

```{r}
lagos1 <- data.table::fread("nhd/epi_waterquality10873.csv")
lagos2 <- data.table::fread("nhd/lakeslimno10873.csv")

lagosne <- left_join(lagos1, lagos2, by = "lagoslakeid")


lagos_sf <- st_as_sf(lagosne, coords = c("nhd_long", "nhd_lat"), crs = st_crs("WGS84"))

lagos_adk <- st_intersection(lagos_sf, st_transform(adkalt, "WGS84"))

# saveRDS(lagos_adk, "data/lagos_adk")

filter(lagosne, nhdid %in% lagos_adk$nhdid) %>% 
  write.csv("C:/Users/borre/Desktop/lagos_adk.csv")
```

```{r lagosMAP, fig.align='center', fig.height = 3.5, fig.width = 3.5, fig.cap = "Location of lakes in the LAGOS-NE-LIMNO dataset"}
ggplot(st_transform(adkalt, "WGS84")) + geom_sf() + 
  geom_sf(data = lagos_adk) + 
  theme_minimal() 
```

```{r eval = FALSE}
# – NC1: has detection limit and data value is above detection limit and has no qualifier or
# comments
# – NC2: has detection limit and data value is above detection limit, and has qualifier or
# comments
# – NC3: has no detection limit and has qualifier or comments
# – NC4: has no detection limit and has no qualifiers or comments
# – LE1: has detection limit, data value is less than or equal to detection limit, has qualifier
# or comments
# – LE2: has detection limit, data value is less than or equal to detection limit, has no qualifier
# or comments
# – LE3: has no detection limit, < than comes from source program, has qualifier or comments
# – LE4: has no detection limit, < than comes from source program, has no qualifier or
# comments
```

The max depth in LAGOS-NE-LIMNO lakes ranges from 0.6 m to 50 m, and mean depth ranges from 0.3 m to 21.3 m. 

```{r lagosDEPTHs, fig.align = "center", fig.height = 5, fig.width = 5, fig.cap = "Mean and max depth in lagos lakes"}
h1p <- ggplot(lagos_adk, aes(x = meandepth)) + geom_histogram() +  
  theme_bw() + 
  labs(x = "Mean Depth (m)", y = "Count")
h2p <- ggplot(lagos_adk, aes(x = maxdepth)) + geom_histogram() +  
  theme_bw() + 
  labs(x = "Max Depth (m)", y = "Count")

h1p/h2p
```


We can also look at trends in multiple values from the LAGOS-NE-LIMNO dataset. 

```{r trendLAGOS, fig.cap = "Secchi depth, color, DOC, chlorophyll a, total phosphorus, and total nitrogen in LAGOS-NE lakes within the Adirondack Park. Lines are output from a Generalized Additive Model.", fig.align = "center", fig.height = 8, fig.width = 5}

lagdoc <- ggplot(filter(lagos_adk, !grepl("LE", doc_censorcode), year(ymd(sampledate)) > 1984), 
                 aes(y = (doc), x = ymd(sampledate))) + 
  geom_point() + 
  geom_smooth() +  
  theme_bw() + 
  labs(x = "Date", y = "DOC")

longsamples <- lagos_adk %>% filter(!grepl("LE", doc_censorcode), !is.na(doc)) %>% group_by(lagosname1) %>% 
  summarize(n = n()) %>% arrange(desc(n))

ggplot(filter(lagos_adk, !grepl("LE", doc_censorcode), year(ymd(sampledate)) > 1984,
                        lagosname1 %in% longsamples$lagosname1[1:30]), 
                 aes(y = (doc), x = ymd(sampledate))) + 
  geom_point() + 
  theme_bw() + 
  labs(x = "Date", y = "DOC") + 
  facet_wrap(~lagosname1, scales = "free_y")

filter(lagos_adk, !grepl("LE", doc_censorcode), year(ymd(sampledate)) > 1984,
                        programid %in% 44) %>% 
  group_by(lagosname1) %>% filter(n() > 10) %>% 
  ggplot(aes(y = (doc), x = ymd(sampledate))) + 
  geom_point() + 
  theme_bw() + 
  labs(x = "Date", y = "DOC") + 
  facet_wrap(~lagosname1, scales = "free_y")

lagTRND <- filter(lagos_adk, !grepl("LE", doc_censorcode), year(ymd(sampledate)) > 1984,
                        lagosname1 %in% longsamples$lagosname1[1:30]) %>% 
  group_by(lagosname1) %>% tidyr::nest() %>% 
  mutate(fitDOC = purrr::map(data, function(x) lm(doc ~ year(ymd(sampledate)), data = x)),
         fitCHLA = purrr::map(data, function(x) lm(chla ~ year(ymd(sampledate)), data = x))) %>% 
  mutate(pDOC = purrr::map_dbl(fitDOC, function(x) summary(x)$coef[2,4]),
         pCHLA = purrr::map_dbl(fitDOC, function(x) summary(x)$coef[2,4]),
         sDOC = purrr::map_dbl(fitDOC, function(x) coef(x)[2]),
         sCHLA = purrr::map_dbl(fitDOC, function(x) coef(x)[2]))

longsamples <- lagos_adk %>% filter(!grepl("LE", chla_censorcode), !is.na(chla)) %>% group_by(lagosname1) %>% 
  summarize(n = n()) %>% arrange(desc(n))

adklakeSUMMARY$lagosname1[adklakeSUMMARY$altm == 1]

lagchl <- ggplot(filter(lagos_adk, !grepl("LE", chla_censorcode), year(ymd(sampledate)) > 1984),
                 aes(y = (chla), x = ymd(sampledate))) + geom_point() + 
  geom_smooth() +    
  theme_bw() + 
  labs(x = "Date", y = "Chl a")

lagtp <- ggplot(filter(lagos_adk, !grepl("LE", tp_censorcode), year(ymd(sampledate)) > 1984), 
                aes(y = (tp), x = ymd(sampledate))) + geom_point() + 
  geom_smooth() +    
  theme_bw() + 
  labs(x = "Date", y = "Total Phosphorus")

lagcolr <- ggplot(filter(lagos_adk, !grepl("LE", colort_censorcode), year(ymd(sampledate)) > 1984),
                  aes(y = (colort), x = ymd(sampledate))) + geom_point() + 
  geom_smooth() +    
  theme_bw() + 
  labs(x = "Date", y = "Color T")

lagsecc <- ggplot(filter(lagos_adk, !grepl("LE", secchi_censorcode), year(ymd(sampledate)) > 1984),
       aes(y = (secchi), x = ymd(sampledate))) + geom_point() + 
  geom_smooth() +    
  theme_bw() + 
  labs(x = "Date", y = "Secchi")


lagtkn <- ggplot(filter(lagos_adk, !grepl("LE", tkn_censorcode), year(ymd(sampledate)) > 1984), 
                 aes(y = (tn), x = ymd(sampledate))) + geom_point() + 
  geom_smooth() +    
  theme_bw() + 
  labs(x = "Date", y = "Total Nitrogen")

(lagsecc / lagcolr) | (lagdoc / lagchl) | (lagtp / lagtkn)

```

```{r, eval = FALSE}
la_samps <- lagos_adk %>% group_by(lagoslakeid, nhdid) %>% 
  summarize(n = n(), start = min(sampledate), end = max(sampledate))

sum(la_samps$n > 10)
lagostrendlakes <- la_samps$nhdid[(la_samps$n > 10)]

filter(lagos_adk, nhdid %in% lagostrendlakes, !is.na(doc)) %>% 
  ggplot(aes(x = sampledate, y = doc)) + geom_point() + facet_wrap(~nhdid)

doctrends <- lagos_adk %>% group_by(lagoslakeid, nhdid) %>% 
  filter(!is.na(doc)) %>% 
  summarize(n = n(), start = min(sampledate), end = max(sampledate)) %>% 
  filter(n > 10)
chltrends <- lagos_adk %>% group_by(lagoslakeid, nhdid) %>% 
  filter(!is.na(chla)) %>% 
  summarize(n = n(), start = min(sampledate), end = max(sampledate)) %>% 
  filter(n > 10)


filter(lagos_adk, nhdid %in% doctrends$nhdid) %>% 
  ggplot(aes(x = sampledate, y = doc)) + geom_point() + facet_wrap(~nhdid)


filter(lagos_adk, nhdid %in% chltrends$nhdid) %>% 
  ggplot(aes(x = sampledate, y = chla)) + geom_point() + facet_wrap(~nhdid)
```

```{r, eval = FALSE}
adksumm <- lagos_adk %>% group_by(lagoslakeid, nhdid) %>% 
  summarize(n = n(), start = min(sampledate), end = max(sampledate),
            chla = mean(chla, na.rm = TRUE),
            doc = mean(doc, na.rm = TRUE), 
            toc = mean(toc, na.rm = TRUE), 
            tp = mean(tp, na.rm = TRUE),
            colort = mean(colort, na.rm = TRUE), 
            secchi = mean(secchi, na.rm = TRUE), 
            tkn = mean(tkn, na.rm = TRUE))

arrange(adksumm, desc(toc))


mvalue <- lagos_adk %>% as.data.frame() %>% 
  select(-ends_with("methodname"), -ends_with("methodinfo"), 
         -ends_with("detectionlimit"), -ends_with("qual"), 
         -ends_with("censorcode")) %>% 
  select(eventida10873:lagosname1) %>% 
  gather(key = var, value = value, chla:secchi)


ccode <- lagos_adk %>% as.data.frame() %>% 
  select(eventida10873 ,lagoslakeid, programid, 
         sampledate, programname, programtype,
         nhdid, lagosname1, ends_with("censorcode")) %>%  
  gather(key = var, value = code, chla_censorcode:secchi_censorcode) %>% 
  separate(var, into = c("var", "type"), sep = "_") %>% 
  select(-type)

lagdat <- left_join(mvalue, ccode, by = c("eventida10873", "lagoslakeid",
                                          "programid", "sampledate",
                                          "programname", "programtype", 
                                          "nhdid", "lagosname1", "var"))

lagsummary <- lagdat %>% filter(!grepl("LE", code)) %>% 
  group_by(lagoslakeid, nhdid, var) %>%
  summarize(meanvalue = mean(value, na.rm = TRUE),
            medianvalue = median(value, na.rm = TRUE), 
            maxvalue = max(value, na.rm = TRUE), 
            minvalue = min(value, na.rm = TRUE), 
            n = n(),
            strt = min(sampledate),
            end = max(sampledate)) %>% 
  filter(!is.nan(meanvalue))

lswide <- lagsummary %>% 
  pivot_wider(names_from = var, values_from = c(n, strt, end, meanvalue, medianvalue, maxvalue, minvalue))

test <- select(ungroup(lswide), starts_with("meanvalue"), -ends_with("tkn"), -ends_with("toc"),-ends_with("dkn"), -ends_with("colora"), 
       -ends_with("tdn"), -ends_with("tdp"), -ends_with("srp"),-ends_with("tn"),-ends_with("secchi"),-ends_with("chla"))

scale(test[complete.cases(test),]) %>% princomp() %>% biplot()

ael <- adkl8 %>% as.data.frame() %>% select(Permanent_, elevation_est, AreaSqKm) %>% 
  group_by(Permanent_) %>% summarize(elev = mean(elevation_est), area = mean(AreaSqKm))

test <- lswide %>%
  left_join(ael, by = c("nhdid" = "Permanent_")) %>% 
  ungroup() %>% 
  select(starts_with("medianvalue"), -ends_with("tkn"),
         -ends_with("toc"),-ends_with("dkn"), -ends_with("colora"), 
         -ends_with("tdn"), -ends_with("tdp"), -ends_with("srp"),
         -ends_with("tn"),-ends_with("secchi"),-ends_with("chla"), area, elev) 
  
  
scale(test[complete.cases(test),]) %>% princomp() %>% summary()

```

```{r, eval = FALSE}
quantile(als_wchem$DOC, na.rm = TRUE)
quantile(lagos_adk$doc, na.rm = TRUE)
quantile(lagos1$doc, na.rm = TRUE)

quantile(altm$DOC, na.rm = TRUE)

quantile(aeapchem$DOC, na.rm = TRUE)

```

## Combining

I started with the NHDplus and LAGOS-NE data. Then I identified AEAP lakes and ALSC lakes by their legacy id value in LAGOS. For EPA TIME and NY CSLAP I linked the nhdid value in LAGOS with the Permanent_ id in NHDplus. Not all ponds/lakes identified in the ALSC database were found in the LAGOS database, so there may be some missing in the full dataset here. The ALSC location dataset included 4833 unique pond numbers, but only 1221 unique ponds/lakes are identified in LAGOS from that survey (from metadata seems like I missed 45?). We also inlcuded identifiers for lakes in the current SCALE pilot study.  

We have included the NHDid (Permanent_), NHDPlusID, GNIS_ID, GNIS_Name, PONDNO, PONDNAME, and Lagos Name, though many of the names are redundant. 
We estimated the elevation of each lake in the NHD dataset using a digital elevation model, averaging cells that overlap the lake shape. 

The HydroLAKES dataset includes estimates of depth, volume, shoreline characteristics, residence time, discharge and watershed area for most lakes  >10 ha. Of the 889 lakes >= 10 ha, depth estimates are inlcuded for all but 155. 

```{r}
adklakeSUMMARY <- readRDS("adkl11.rds")
```

```{r lakesPerProgram}
nlak <- select(as.data.frame(adklakeSUMMARY), aeap:awi) %>% colSums()
data.frame(program = names(nlak), lakes = nlak) %>% 
  ggplot(aes(x = reorder(program, lakes), y = lakes)) + geom_bar(stat = "identity") + coord_flip() + 
  theme_bw() + 
  labs(x = "", y = "Number of Lakes")
```

```{r mchla, fig.height = 3, fig.width = 3, fig.cap = "Mean chlorophyll across all measurements for each lake"}
a1 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_chla)) + geom_histogram() 
a1
```


```{r mp, fig.height = 6, fig.width = 3, fig.cap = "Mean phosphorus across all measurements for each lake"}
a2 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_tp)) + geom_histogram() #+ scale_x_log10()
a3 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_tdp)) + geom_histogram() #+ scale_x_log10()
a4 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_srp)) + geom_histogram() + scale_x_log10()

a2 / a3
```


```{r mn, fig.height = 9, fig.width = 5, fig.cap = "Mean nitrogen across all measurements for each lake"}
a5 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_tn)) + geom_histogram() + scale_x_log10()
a6 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_tdn)) + geom_histogram() + scale_x_log10()
a7 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_no2no3)) + geom_histogram() + scale_x_log10()
a8 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_nh4)) + geom_histogram() + scale_x_log10()
a9 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_dkn)) + geom_histogram() + scale_x_log10()

(a5 | a6) / (a7 | a8 | a9)

```


```{r mdoc, fig.height = 9, fig.width = 5, fig.cap = "Mean secchi, carbon, and color across all measurements for each lake"}
a10 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_doc)) + geom_histogram() + scale_x_log10()
a11 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_toc)) + geom_histogram() + scale_x_log10()
a12 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_colort)) + geom_histogram() + scale_x_log10()
a13 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_colora)) + geom_histogram() + scale_x_log10()
a14 <- adklakeSUMMARY %>% 
  ggplot(aes(x = meanvalue_secchi)) + geom_histogram() + scale_x_log10()

a14 / (a10 | a11) / (a12 | a13)
```

```{r trends, fig.height = 3, fig.width = 3, fig.cap = "Trend in water clarity across 790 lakes in ADK"}
adklakeSUMMARY %>% 
  ggplot(aes(x = trend)) + geom_histogram()
```


# Modelling

## Generic lake models

We simulate lakes with systematically varying characteristics to determine how max depth, mean depth, surface area, and Kd interact to determine thermal regime. 

```{r}
tdf <- arrow::read_parquet("data/lakechar.parquet")
tdf <- arrow::read_parquet("../lakeshapes/lakechar.parquet")
```

```{r}
tdf %>% filter(month(date) %in% c(8),
               year(date) == 2020, elev == 500) %>% 
  filter(meandep %in% c(2, 5, 15, 25)) %>% 
  ggplot(aes(x = kd, y = cb, fill = factor(area), group = paste(kd, area))) + geom_boxplot() + 
  facet_grid(maxdep ~ meandep, scales = "free_y") 


tdf %>% filter(lubridate::month(date) %in% c(7), lubridate::year(date) == 2020) %>% 
  filter(meandep %in% c(25)) %>% 
  #group_by(model, area, maxdep, meandep, kd, shape, elev) %>% 
  filter(model == "GLM", shape == "circle") %>% 
  #summarize(cb = mean(cb, na.rm = TRUE)) %>% 
  ggplot(aes(x = kd, y = ss, color = factor(maxdep))) + 
  stat_summary(fun.data = mean_se) + 
  stat_summary(fun.data = mean_se, geom = "line") + 
  facet_grid(elev ~ area, scales = "free_y")

# data.table::fwrite(data.table::rbindlist(lakevalues), "lakechar.csv")
# lakechar <- data.table::fread("lakechar.csv")

tdf %>% filter(lubridate::month(date) %in% c(6:8), shape == "circle", model == "GLM",
               area == 1000, elev == 500, maxdep == 30, meandep == 15) %>% 
  filter(meandep %in% c(2, 5, 15, 25)) %>% 
  mutate(rat = paste(meandep,maxdep), 
         rat = factor(rat, levels = c("2 5", "2 15", "2 30", "2 50", "5 15", "5 30", "5 50", "15 30", 
                                      "15 50", "25 30", "25 50"))) %>% 
  ggplot(aes(x = lubridate::yday(date), y = cb, color = factor(kd))) + geom_point() + 
  facet_grid(lubridate::year(date) ~ meandep, scales = "free_x") 


tdf %>% filter(month(date) %in% c(6:8), shape == "circle", elev == 500, year(date) == 2020) %>% 
  filter(meandep %in% c(2, 5, 15, 25)) %>% 
  ggplot(aes(x = lubridate::yday(date), y = cb, color = factor(kd))) + geom_point() + 
  facet_grid(meandep ~ maxdep, scales = "free_y")
```


```{r}
tdf2 <- tdf %>% filter(meandep %in% c(2, 5, 15, 25))
tdf2$year <- lubridate::year(tdf2$date)
tdf2$month <- lubridate::month(tdf2$date)
tdf2 <- tdf2 %>% 
  group_by(area, maxdep, meandep, kd, width, length, elev, year, model) %>% 
  filter(d > 0.5) %>% 
  summarize(sstrt = min(date), ends = max(date))

filter(tdf2, meandep > 2) %>% 
  gather(key = time, value = date, sstrt:ends) %>% 
  ggplot(aes(x = kd, y = yday(date), group = meandep, color = factor(meandep))) + 
  stat_summary(fun.data = mean_se) + 
  stat_summary(fun.data = mean_se, geom = "line") + 
  facet_grid(time~maxdep, scales = "free_y")


filter(tdf2, meandep > 2) %>% 
  mutate(ndays = as.numeric(yday(ends) - yday(sstrt))) %>% 
  ggplot(aes(x = kd, y = ndays, group = meandep, color = factor(meandep))) + 
  stat_summary(fun.data = mean_se) + 
  stat_summary(fun.data = mean_se, geom = "line") + 
  facet_grid(model~maxdep, scales = "free_y")
```

```{r}

#kd from https://onlinelibrary.wiley.com/doi/full/10.1046/j.1365-2427.2000.00518.x
test <- adklakeSUMMARY %>% as.data.frame() %>% 
  select(Permanent_, best_maxdepth, best_meandepth, AreaSqKm, elevation_est, meanvalue_doc, meanvalue_secchi) %>% 
  filter(!is.na(best_maxdepth), !is.na(best_meandepth), best_meandepth < best_maxdepth) %>% 
  filter(!is.na(meanvalue_doc)) %>% #st_zm() %>% 
  mutate(area_sqm = AreaSqKm * 1000000, len = sqrt((area_sqm)/pi) * 2, wid = len, 
         kd = (meanvalue_doc * 0.15)^1.08)

test

dmax <- quantile(adklakeSUMMARY$best_maxdepth, probs = seq(0.05,0.99,length.out = 5), na.rm = TRUE)
dmean <- quantile(adklakeSUMMARY$best_meandepth, probs = seq(0.05,0.99,length.out = 5), na.rm = TRUE)
larea <- quantile(adklakeSUMMARY$AreaSqKm, probs = seq(0.05,0.95,length.out = 5), na.rm = TRUE) * 1000000
kdval <- (quantile(adklakeSUMMARY$meanvalue_doc, probs = seq(0.05,0.95,length.out = 5), na.rm = TRUE) * 0.15)^1.08
```

## AEAP

We simulate the time series of 1D temperature profiles from 1992 through 2012 for each of the 28 long-term monitoring lakes to examine patterns of stratification. 